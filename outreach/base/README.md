# base/

Your base email, saved verbatim before anything is built on it.

`/email-base` writes here. Verbatim matters: `/email-learn` diffs against it
later, and an already-improved "original" destroys that signal.

Tracked - so no recipient names in here. The base email is about you and the
software, not about any one reader.
