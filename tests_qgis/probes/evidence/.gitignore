# Intermediate rasters and cached arrays a probe writes while measuring — conditioned
# surfaces as .npy, synthetic DEMs, round-trip GeoTIFFs. Regenerated by re-running the
# probe, ~9 MB, and of no use to a reader. The evidence *files* beside this one are the
# deliverable and ARE tracked.
_work/
